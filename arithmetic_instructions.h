#ifndef ARITHMETIC_H
#define ARITHMETIC_H

#include <math.h>
#include "types.h"
#include "helper.h"

int iadd(Frame * frame);
int ladd(Frame * frame);
int fadd(Frame * frame);
int dadd(Frame * frame);




#endif // ARITHMETIC_H